import { Bar as RechartsBar } from "recharts";

/**
 *  NOTE: use the recharts Bar as it is, here is posible
 *  to modify or secure the props that will been pass to the component
 */
export default RechartsBar;
